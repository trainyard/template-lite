{
  "name": "<%= projectName %>",
  "version": "<%= projectVersion %>",
  "description": "<%= projectDescription %>",
  "license": "<%= projectLicense %>",
  "scripts": {
    "build:prod": "scripts/build-prod.sh",
    "start": "scripts/dev-server.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "choo": "^3.2.0"
  },
  "devDependencies": {
    "budo": "8.3.0",
    "choo-log": "^1.4.0",
    "es2040": "1.2.2",
    "normalize.css": "^4.2.0",
    "sheetify": "^5.0.3"
  }
}
